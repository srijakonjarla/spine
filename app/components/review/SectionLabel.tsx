/** Small uppercase section heading used across the year-review page. */
export function SectionLabel({ children }: { children: React.ReactNode }) {
  return <p className="section-label mb-5">{children}</p>;
}
